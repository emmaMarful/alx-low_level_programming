#include "main.h"

/**
 * mul - a function to multiply two integers
 * @a: an argument variable in mul
 * @b: an argument variable in mul
 * Return: multiple
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
