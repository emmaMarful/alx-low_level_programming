#include <stdio.h>
#include "main.h"

/**
 * main - a program that prints its name
 * @argc: number of arguments
 * @argv: name of array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
